<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="4dp"
        android:color="#FF6200EE" /> <!-- Màu viền -->
    <corners android:radius="16dp" /> <!-- Bán kính bo -->
</shape>
